const express = require("express");
const z = require("zod");

let reviewPostSchema;

module.exports = function(){
  router.post("/", async function() {
    //Fill out for creating a review controller
  });

  router.get("/:tvShowId", async function(req, res){
    //Fill out for getting reviews based on a tv show
  });
}
